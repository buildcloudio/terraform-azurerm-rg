name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    uses: buildcloudio/terraform-registry-template/.github/workflows/module-release.yml@main
    permissions:
      contents: write
    secrets:
      REGISTRY_API_KEY: ${{ secrets.REGISTRY_API_KEY }}